import 'package:flutter/material.dart';

import 'models/habit.dart';

class BottomSheetExample extends StatelessWidget {
  final Habit habit;
  const BottomSheetExample({super.key, required this.habit});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}

